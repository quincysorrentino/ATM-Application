<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- url -->
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/atm_db</property>
        <!-- username -->
        <property name="hibernate.connection.username">root</property>
        <!-- password -->
        <property name="hibernate.connection.password">5h6pHo-=Esw!trU</property>
        <!-- mysql driver -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <!-- show sql query -->
        <property name="hibernate.show_sql">false</property>  <!--change to true to display sql-->
        <!-- entity mapping -->
        <mapping class="org.example.User"/>
        <mapping class="org.example.Transaction"/>
    </session-factory>
</hibernate-configuration>

